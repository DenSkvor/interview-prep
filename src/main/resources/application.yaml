server:
  port: 8189

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/students2_db?serverTimezone=Europe/Moscow
    username: root
    password: admin
    show-sql: true
    properties:
      hybernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    org.springframework.web: DEBUG
